/**
 * Mock Proposals Hook (Dev Only)
 *
 * Re-exports mock data from centralized fixtures.
 * Replace with contract reads in production.
 */

export { MOCK_ESCROW_BALANCE, MOCK_PROPOSALS, MOCK_TOTAL_SUPPLY } from "@/fixtures";
