# GitHub OAuth
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Facebook OAuth
FACEBOOK_APP_ID=
FACEBOOK_APP_SECRET=

# EAS (Base mainnet)
EAS_CONTRACT_ADDRESS=0x4200000000000000000000000000000000000021
EAS_SCHEMA_UID=

# Signer private key for creating attestations
ATTESTATION_SIGNER_KEY=

# Reclaim Protocol (zkTLS for tweet verification)
RECLAIM_APP_ID=
RECLAIM_APP_SECRET=

# LLM (for chat agent)
ANTHROPIC_API_KEY=

# Privy (authentication — get from dashboard.privy.io)
PRIVY_APP_ID=
PRIVY_APP_SECRET=

# Deployer (server-side wallet for gas-sponsored token launches)
# Fund this wallet with ETH on Base for gas (~$0.01-0.05 per launch)
DEPLOYER_PRIVATE_KEY=
SIGIL_FACTORY_ADDRESS=

# Fee infrastructure (on-chain contracts)
SIGIL_FEE_VAULT_ADDRESS=
SIGIL_LP_LOCKER_ADDRESS=
# Legacy V1 contracts (optional — only needed if migrating from V1)
# SIGIL_FEE_VAULT_ADDRESS_V1=
# SIGIL_LP_LOCKER_ADDRESS_V1=

# Custodial wallets (AES-256 encryption key for private key storage)
# Generate with: openssl rand -hex 32
WALLET_ENCRYPTION_KEY=

# Trading (0x Swap API — free tier, optional)
ZEROX_API_KEY=

# Database
DATABASE_URL=postgresql://localhost:5432/sigil

# Base RPC (for contract deployment + schema registration)
BASE_RPC_URL=https://sepolia.base.org

# Server
PORT=3001
BASE_URL=http://localhost:3001
FRONTEND_URL=http://localhost:3000

# Railway provisioning (optional; required for /connect quick-launch deployment)
RAILWAY_API_TOKEN=
RAILWAY_PROJECT_ID=
RAILWAY_ENVIRONMENT_ID=
RAILWAY_SOURCE_REPO=heysigil/heysigil
RAILWAY_SOURCE_BRANCH=main
RAILWAY_SOURCE_ROOT_DIR=packages/sigilbot
RAILWAY_SERVICE_DOMAIN_SUFFIX=up.railway.app
RAILWAY_SERVICE_NAME_PREFIX=sigilbot
RAILWAY_MIN_CPU_MILLICORES=100
RAILWAY_MIN_MEMORY_MB=256

# X Agent (Twitter bot for token launches via @mentions)
TWITTER_API_KEY=
TWITTER_API_SECRET=
TWITTER_ACCESS_TOKEN=
TWITTER_ACCESS_TOKEN_SECRET=
X_AGENT_BOT_HANDLE=HeySigilBot
X_AGENT_POLL_INTERVAL_MS=30000
X_AGENT_SIGIL_API_URL=https://heysigil-production.up.railway.app
X_AGENT_MCP_TOKEN=
X_AGENT_DRY_RUN=false
