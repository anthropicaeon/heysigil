[build]
builder = "DOCKERFILE"
dockerfilePath = "packages/sigilbot/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
# Minimum CPU/memory profile is enforced by Sigil backend Railway provisioner.
